---
all:
  children:
    domain_vms:
      hosts:
        # Les VMs seront ajoutées dynamiquement ou manuellement ici
        # Exemple :
         UBUNTU-DEV1:
           ansible_host: <ip_vm>
         UBUNTU-DEV2:
           ansible_host: <ip_vm>
         UBUNTU-DEV3:
           ansible_host: <ip_vm>
      vars:
        ansible_user: ubuntu
        ansible_ssh_private_key_file: ~/.ssh/id_rsa
        ansible_python_interpreter: /usr/bin/python3
        ansible_become: yes
        ansible_become_method: sudo
